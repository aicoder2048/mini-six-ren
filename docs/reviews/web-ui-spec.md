# Web 界面改进：Spec 独立审查

- 日期：2026-09-15。
- 审查者：独立 Spec Agent，未参与实现。
- 规格：`.scratch/web-ui/spec.md`；任务：`.scratch/web-ui/issues/01-interface.md`。
- 基线：`dbf6565c8d582c41e1ade1e3fb04b669468b3883`。
- 范围：`git diff <baseline>` 中的 `src/web.py`、`tests/test_regressions.py`、`README.md`，以及 `git ls-files --others --exclude-standard` 发现的 `src/web.css` 和任务规格文件。

## 结论

阻断问题 0，非阻断问题 0；未发现规格遗漏、错误实现或超出 UI 改善范围的行为变更。

最终样式补查通过：问题输入框选择器提高优先级，使 80px 最小高度生效；900px 以下的说明栏使用 `42px minmax(0, 1fr)` 网格，文案占第二列、按钮另起一行并左对齐第二列，消除按钮挤压文案的问题。文字列使用 NiceGUI 实际的 `.nicegui-column` 类并设置 `min-width: 0`，符合窄屏换行目标。已对照安装版本的组件类名和 Quasar 样式；600px 断点不覆盖该网格规则。README 新增视觉、响应式及 `web.css` 说明与实现一致；以上调整不改变计算或 AI 行为，原结论维持。

## 验证证据

- “暖白纸感背景、墨色文字与朱砂主色”：本地 CSS 变量、系统中文衬线字体与导航、简介、表单、阅读区、页脚均已实现，没有新增外部字体或图片请求。
- “桌面并列展示……窄屏顺序排列”：工作区使用两列网格，900px 以下切换为单列；三传卡片、时间输入设有窄屏断点，长结果文本允许换行。
- “默认值和合法范围不变”：比对原版数字默认值 1/2/3、范围 1–999，日期范围 1900–2099 与北京时间初始化，以及汉字输入和模型选项，保持原契约。
- “初始结果区……完整展示”：新增空状态及三阶段说明；结果继续显示输入快照、问题、全部符号释义、两段有向关系与可展开文化背景；指南具有打开、关闭回调。
- “等待、错误、完成状态仍可访问”：保留 `role=status`、`aria-live=polite`、`role=alert`，CSS 提供焦点样式与减少动态效果规则。
- 独立 AST 比对证实：初始化、模型发现、三种验证函数、错误处理、模型切换及逐客户端建页函数不变；提交函数仅改变等待动画颜色，计算与 AI 调用流程不变。本次未涉及算法变更。
- `uv run python -m unittest discover -s tests -v`：30 项全部通过，覆盖日期/汉字取数、无密钥与空问题、AI 失败保留本地结果、重复提交及客户端状态隔离。客户端断言调整为保留各自初始空状态，仍验证隔离。

## 覆盖限制

此审查覆盖静态实现、原版行为比较与自动化回归；真实浏览器断点、横向溢出、键盘焦点和指南交互由实现 Agent 另行验收。本次未调用真实 AI 服务，也未进行辅助技术朗读测试。
