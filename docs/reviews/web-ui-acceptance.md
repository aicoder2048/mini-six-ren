# Web 界面改进验收

- 日期：2026-09-15
- 规格：`.scratch/web-ui/spec.md`
- 基线：`dbf6565c8d582c41e1ade1e3fb04b669468b3883`，开始时工作树干净。

## 交付

页面采用暖白背景、朱砂主色和系统中文衬线标题。桌面分为起课面板与三传阅读区；900px 以下改为纵向布局。新增三传空状态与可开关的使用指南；保留完整结果、输入快照、方向关系、文化背景和 AI 状态。样式集中在 `src/web.css`，按模块路径加载。

## 验证证据

- `uv run python -m unittest discover -s tests -v`：30 项通过，包括三种输入、客户端隔离、重复提交、AI 失败保留结果。隔离测试以第二个客户端的初始内容为基准，适配新增空状态。
- `uv lock --locked`：通过，无依赖变更。
- `git diff --check`：通过。
- 本地服务：`uv run python src/web.py`。
- 内置浏览器连接未提供可用实例；使用本机 Playwright 和独立 headless Chrome 对 `http://localhost:8080/` 进行页面验收，不复用用户浏览器会话。
- 实际点击验证：指南打开/按钮关闭/Escape 关闭；数字 1、2、3 得到大安、留连、赤口，并完整显示“金克木”；文化背景展开且内容可见。
- 日期输入 2026-09-15 12:00 的数字快照为 8、5、7；汉字“中国人”为 4、8、2；非法汉字输入显示错误，修正后可成功重试且清除错误。
- 在 1440、1024、900、768、390、320px 宽度检查结果页：文档宽度未超过视口，无水平溢出。
- 390px 手机视口验证空状态、时间输入和指南；问题输入可键盘填写，Tab 可进入模型选择框。
- 浏览器未捕获页面脚本异常。人工检查桌面空状态、桌面结果与手机空状态、手机结果截图，完成徽章配色、问题框高度和手机提示区布局修正。
- 临时浏览器脚本与截图位于本机 `/tmp/mini-six-ren-ui/`，不纳入仓库依赖或长期测试套件。

## 独立审查

- [Standards](web-ui-standards.md)：通过，0 阻断、0 非阻断问题。
- [Spec](web-ui-spec.md)：通过，0 阻断、0 非阻断问题。
- 两位独立审查者均以 AST 对照确认计算、输入解析、历法、模型调用行为未变；本次没有算法变更。

## 限制

未调用真实在线 AI；AI 成功/失败行为由现有离线测试覆盖。未对 Safari、Firefox、实体手机和屏幕阅读器作完整验证。截图使用本机可用中文字体，其他平台的字形可能不同。
