# 三传 Prompt — Spec 独立复核

结论：通过，无阻断项。Reviewer 未参与代码实现；此前仅提供只读问题分析。本报告不代表真实模型质量已经提高。

## 范围与符合性

对照基线 `9b44682`，检查 `git diff 9b44682` 及全部任务新增文件，以 [Prompt质量规格](../specs/prompt-quality.md) 为依据：

- 要求1、3、5：明确三传联读、目标差异、事实与假设、重复主题；两段已有关系承接主线，不添加首末关系或流派规则。未发现缺失、错误实现或相互冲突。
- 要求2、4、6：承接已知事实，未知用条件表达；用户时间范围不作为应期。各传建议限一句，末尾提炼两项有先后与调整条件的行动，与去重要求兼容；五标题、字数目标和预算保留。
- 要求7、8：9个场景覆盖全部指定情境；具有正向判据、失败信号、五维评分、硬失败、配对盲评和版本记录。01/02、03/04的成对检查能区分套话；已见示例与迁移场景明确区分。
- 未发现范围扩张。算法、数据、UI、依赖与输入映射没有修改；AST比较将 `_get_system_prompt` 函数体归一化后，基线与当前 `ai_agent.py` 完全一致，确认请求JSON、模型、预算和调用路径未变。

## 独立验证证据

依据既有九宫顺序手工包含起点计数，以固定期望逐例调用 `HandTechnique.predict`，9例全部一致；关系方向单独按相生相克核对：

| 场景 | 数字 | 三传 | 关系 |
|---|---|---|---|
| 01/02 | 1,2,3 | 大安木→留连木→赤口金 | 比和、被克 |
| 03/04 | 1,1,1 | 大安木→大安木→大安木 | 比和、比和 |
| 05 | 6,1,1 | 空亡土→空亡土→空亡土 | 比和、比和 |
| 06 | 8,5,7 | 桃花土→速喜火→天德金 | 被生、克 |
| 07 | 3,5,8 | 速喜火→病符土→小吉水 | 生、克 |
| 08 | 9,9,9 | 天德金→桃花土→病符土 | 被生、比和 |
| 09 | 7,1,1 | 病符土→病符土→病符土 | 比和、比和 |

`uv run python -m unittest discover -s tests -v`：30项通过。未调用在线模型。

## 限制与未解决项

无未解决阻断项。固定结构仍可能产生语义重复、示例套用或字数偏离；这是待真实A/B评测观察的行为，离线通过不保证输出质量。医疗场景的“文化解释简短”可以在固定结构内实现，但实际冗长度仍需人工检查。未对未变算法重新作全面正确性证明。
