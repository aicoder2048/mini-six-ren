# 算法独立复核记录

日期：2026-09-15。Reviewer：独立 Agent `algorithm_reviewer`，未参与实现，也未修改项目算法文件。

范围：当前工作树相对基线 `3e6014e` 的算法改动。依据：`docs/specs/review-improvements.md` 和 `specs/codebase-overview.md`。

## 结论

**独立复核通过，未发现未解决的本次算法变更阻断项。** Reviewer 发现一个公共 API 校验问题，由实现 Agent 修复并增加回归测试，随后由同一 Reviewer 独立复查通过。

## 发现、修复与复审

**P2：`lunar_to_solar` 接受分数日期。** 修复前，`lunar_to_solar(2024, 1, 1.5)` 返回 `(2024, 2, 11)`，而不是拒绝无效日期。部分布尔日期分量和非布尔闰月标记也可能被隐式接受。

修复位置：`src/utils/calendar_converter.py`。年、月、日现在必须为整数且不能为布尔值，闰月标记必须为布尔值。CLI 原有整数解析使普通界面输入不受此路径影响。

实现 Agent 先执行新增回归测试，观察到4个失败和1个错误，再修复并运行全部23项测试通过。Reviewer 独立重跑七类非法输入，均按预期抛出 `ValueError`。

## 独立验证证据

以下为 Reviewer 独立执行结果，不是实现 Agent 对原测试结果的转述。

| 范围 | 检查与结果 |
|---|---|
| 九宫算法 | 从基线提取旧实现 AST；13,726组三数新旧输出一致，包含729种余数组合、10,000组固定种子随机输入，以及三个位置各自的1–999扫描 |
| 五行关系 | 全部729个符号三元组与旧关系实现一致 |
| 笔画缓存 | 全部40,754个字典项与旧首命中解析一致；无重复键，笔画范围1–64 |
| 公历边界 | 1900–2099每个月的首日和末日共4,800个日期双向转换通过；日柱与参考锚点的连续干支循环一致 |
| 农历边界 | 全范围农历月及74个闰月，4,945个有效端点往返通过；3,488个不存在的闰月或小月三十正确拒绝 |
| 八字参考 | 另选已有测试未覆盖的6个上游公开参考值，全部通过 |
| 节气边界 | 1900、1950、2000、2024、2099各12节，共60个节气相邻分钟检查通过；月柱换月、仅立春换年符合约定 |
| 性别映射 | 两个分析函数对10个天干及M/m/男、F/f/女、前后空白处理一致；非法性别拒绝 |
| CLI/Web一致性 | 实际CLI处理函数与Web转换对照：1900下界、2099上界、2023闰二月23:59、春节01:00，三数一致 |

参考：[上游 EightChar 实现](https://raw.githubusercontent.com/6tail/lunar-python/master/lunar_python/EightChar.py)、[上游参考测试](https://raw.githubusercontent.com/6tail/lunar-python/master/test/EightCharTest.py)。实现显式使用 `setSect(2)`；日柱午夜换日，晚子时的时柱仍遵循该库自身规则。

项目回归命令：`uv run python -m unittest discover -s tests -v`，当前23项通过。上述较大范围的独立采样不等同于这23项常规回归测试。

Reviewer 将独立执行的检查整理为脚本，已保存到 `scripts/audit_algorithm_changes.py`。实现 Agent 在保存后完整运行合并脚本，以上检查全部通过。复现命令（需在项目根目录执行，且 Git 中保留基线提交 `3e6014e`）：

```bash
uv run python scripts/audit_algorithm_changes.py
```

## 保留的既有问题及覆盖限制

- CLI 的“日主分析”工具仍按农历月份映射五行；本次修复的是八字计算主路径，尚未统一该旧工具。
- 男女采用不同配偶宫柱位、克泄耗列表及相关叙事规则仍为旧实现；本次未验证或修订其领域有效性。
- 未逐日穷举200年的所有日期，也未用第二套独立天文历表证明库内全部历法数据。节气检查证明封装遵守库的边界，不等于独立证明天文计算正确。
- 未调用在线AI服务。
