# 三传体验改进：Spec 独立审查

日期：2026-09-15；Reviewer：`spec_review`，未参与实现。基线 `f69f4f809a6ee874e34fcb66d55aa87ce7a942ef`，审查工作树差异及全部新增文件。权威规格：[三传体验](../specs/interpretation-experience.md)。

## 初审发现（均已修复）

1. **P2，部分实现：CLI 缺少起课数字及日期约定。** 规格第 5 条要求“用户能看到本次起课使用的数字；日期模式说明农历月日、时辰、北京时间与闰月约定”。`src/cli.py:445–471` 将日期/汉字映射为三数后直接渲染符号，未展示三数和闰月规则。用本地模式输入 `2026-09-15 12:00`，真实 CLI 输出不含 `8、5、7`、`起课数字` 或 `闰月`；Web 已实现。
2. **P2，看似实现但实际截断：CLI 关系方向显示不完整。** 规格第 4 条要求“CLI与Web用户看到同样的关系含义”，验收要求使用共用方向解释。`src/cli.py:52` 新增长文案，但 `display_divination_result` 在 `src/cli.py:374–379` 将表宽设为终端全宽后放进带边框的 Panel。80 列实际渲染上述日期结果得到“火生土，后传 …”“火克金，前传 …”，末传标题也被截断。既有布局与新增文案组合暴露该问题；直接打印表格的测试没有覆盖真实入口。

## 其余结论与证据

- 未发现未要求的范围扩张。Prompt 明确九宫法、结构化事实及问题字段；五个标题、每传依据/白话/建议、条件性趋势和两个 2400 tokens 请求路径均符合规格。关系方向、九符号语义中性化与兼容性说明一致。
- `uv run python -m unittest discover -s tests -v`：28 项通过。复现额外使用 `Console(file=StringIO(), width=80)`，替换 CLI 的 Console 构造与输入后运行真实 `xiaoliu_submenu`，核对上面的缺失和截断文本。
- Web 客户端的输入快照、错误保留及恢复符合离线测试；浏览器视觉验收由主 Agent 另行记录。未调用在线模型，不保证实际输出质量或抗提示注入效果。算法独立复核见独立记录，本报告不替代它。

## 修复复审

CLI 三种模式现均输出起课数字；日期和汉字保留本次输入，日期明确 UTC+8、闰月与 23 点约定。关系说明移至表格下方两行，取消 Panel 内强制全宽表格。本 Reviewer 再次运行真实 `xiaoliu_submenu`，以 80 列分别输入 `1,2,3`、`2026-09-15 12:00`、`中国人`，确认显示 `1、2、3`、`8、5、7`、`4、8、2`；无省略号，日期结果完整显示“后传生前传”“前传克后传”。两项关闭。

共用 `INTERPRETATION_MAX_TOKENS=2400` 保持两请求路径契约。最终全套 **30 项测试通过**，包括新增真实 CLI 入口及 Panel 检查。

结论：**Spec 独立复审通过，未解决发现 0 项；范围扩张 0 项。** 浏览器验收与流程票据由主 Agent 最终记录，不代表本 Reviewer 独立完成浏览器或在线模型评测。
