# 三传 Prompt 质量改进验收

日期：2026-09-15。基线：`9b44682`；原始工作树干净。规格：[Prompt联读与针对性](../specs/prompt-quality.md)。

## 完成范围

- 加强三传联读：一句话概括主线，中传承接初传，末传联系前两传；保持两段已有关系及方向，不增加领域算法。
- 加强目标与情境匹配：承接已知事实，标明假设，指出关键未知项；重复符号讲共同主题，不强造转折。
- 建议去重：各传建议限一句，最后提炼两项优先行动及反馈条件；保留五标题、篇幅目标及2400 tokens预算。
- 建立[9个固定人工评测场景](../evals/three-transmissions.md)，含7组不同数字、明确表现/失败信号、五维评分、配对盲评步骤与记录模板。

## 验证证据

- `uv run python -m unittest discover -s tests -q`：30项通过，包括两种AI调用路径的请求事实与结构契约。
- `uv lock --locked`：通过，108个包，未改依赖。
- `git diff --check`：通过。
- 对基线与当前 `ai_agent.py` 解析AST，仅将 `_get_system_prompt` 函数体屏蔽后，两份AST一致；运行时代码唯一变化是系统提示文本。计算模块及data目录无diff。
- 两个独立Reviewer核对评测输入对应的符号、五行及关系，9个场景全部一致。

[Standards审查](prompt-quality-standards-review.md)：通过，明确规范违规0项、有依据的异味0项。

[Spec审查](prompt-quality-spec-review.md)：通过，阻断项0项；提示契约、范围与人工评测资料符合规格。

本轮未修改任何算法、数据解析或输入映射，按工作流复用[已有算法独立复核](interpretation-algorithm-review.md)，不声称新增一次算法复核。

## 覆盖限制

系统Prompt文字与离线请求契约的通过，只证明要求已写入并实际发送，不证明模型遵循率或解读质量已提升。9场景真实在线A/B尚未执行，没有质量评分、胜率、成本或时延的实测结论。UI未变，无需重复前轮视觉验收；本轮没有新增字符串逐句匹配的测试。

后续在线评测可按手册执行：相同模型/版本/参数，两个Prompt版本，每例各3次，共54次请求；保留原始响应并做人工配对盲评。当前文档不会自动调用模型或产生请求费用。
