# Codex 项目指令

## 默认语言

- 默认使用简体中文与用户交流，包括进度更新、解释、澄清问题、代码审查和最终回复。
- 即使用户用英文提问，也默认用简体中文回答；用户明确指定其他语言时，以该要求为准。
- 新增或更新的规格、计划和说明文档默认使用简体中文；代码注释遵循所在文件的既有风格。
- 代码标识符、命令、路径、API 名称、日志和错误原文保留原样，必要时附中文解释。

## 项目约定

- 开始修改代码前，阅读根目录的 `CLAUDE.md`，遵循其中的项目架构、开发规约和验证要求。
- `specs/codebase-overview.md` 可作为代码背景参考；其中标注为历史快照的内容应结合当前实现核实。

## 算法变更的独立复核

- 涉及算法新增、修改、替换，或可能影响计算结果的重构时，必须委派一个未参与该次实现的独立 Agent Reviewer 进行复核；实现者自审或仅运行已有测试不能替代此步骤。
- 复核范围包括公式与领域约定、输入输出、边界条件、兼容性及行为变化。更换计算库、调整历法/时区/换日规则、输入映射或数据解析逻辑也属于算法变更。
- 向 Reviewer 提供需求规格、变更范围和可运行的验证命令；要求其独立构造参考样例、边界或性质检查，避免仅用实现自身生成的期望值证明正确。
- 对有意改变的结果核对规格和可靠参考资料；对要求保持不变的行为比较新旧实现。区分本次引入的问题、既有问题和明确不在本次范围内的问题。
- 修复 Reviewer 发现的阻断问题后，必须由该 Reviewer 再次检查受影响部分。独立复核完成前，不得将算法变更报告为已完成。
- 在规格或审查记录中保存复核范围、验证证据、结论、未解决项及覆盖限制，并在最终回复中说明独立复核结果。若无法启动独立 Reviewer，明确标记复核尚未完成。
